# Plimsoll — 前端 UI/UX 設計規格 v2（完整版）

> 產品名：**Plimsoll（載重線）**　·　全稱：Plimsoll — 船舶技術文件版本差異 Agent　·　命名理由見 [`naming.md`](./naming.md)
> 狀態：**設計方向已定案**（見 §9），可進入實作。
> 設計稿：[`ui-mockup-v2.html`](./ui-mockup-v2.html)（12 個畫面）　·　前一版：[`ui-mockup-v1.html`](./ui-mockup-v1.html)（10 個畫面）
> v1 → v2 增量：RWD 手機／平板（§4.6）、字型大小與字體設定（§4.4）、比對高亮色盤（§4.5）、視覺差異比對（§6）
> 依據：`data/specs/船舶技術文件版本差異_Agent_工作包.html`、`data/specs/ship-agent-handoff-prompt.md`、第一版 `app.py`（Streamlit）、現行 `frontend/src/**`

---

## 1. 問題診斷

### 1.1 第一版有、現行 React 版遺失的功能

| # | 功能 | 第一版 `app.py` | 現行 React | 命題對應 |
|---|---|---|---|---|
| F1 | 雙 PDF 上傳 + 「開始比對」 | `st.file_uploader` ×2 + 按鈕 | **完全沒有**，開頁自動打 `/api/demo-data` | AC1 / 第一版必做 #1 |
| F2 | 雙欄 PDF 原文檢視器 + 頁碼跳轉 | `pdf_viewer()` + 差異選擇器 | **沒有** | 「每筆差異 100% 能開啟雙版本來源」 |
| F3 | 元數據不全 → 手動補版本號 | warning + 兩個輸入框 | **沒有**（頁數還寫死 `3 頁`） | R1 / Missing Info 情境 |
| F4 | 語言切換 繁中 / English | sidebar selectbox + `COPY` 字典 | **沒有** | — |
| F5 | API Key 輸入 + 引擎狀態顯示 | 密碼框 +「已啟用 LLM / 本機規則引擎」 | 只剩模型下拉，不知有無生效 | — |
| F6 | HTML 審查報告下載 | `report_html()` + download_button | 按鈕打 `/api/export-html`，**後端無此路由 → 404** | 第一版必做 #5 |
| F7 | 未覆核 High 的匯出攔截 | 紅色 error banner | 只有靜態橫幅，**無二次確認** | AC4 / Human Control 情境 |
| F8 | RAG 問答在主流程 | `st.chat_input` 在主軸 | 縮到右下角浮動泡泡 | — |
| F9 | 掃描頁警示 | 頁面頂部 warning | 只在工程師視圖內 | Exception 情境 |
| F10 | 免責聲明 | 頁面固定顯示 | 只在 footer 一行 | 「結果是草稿與風險提示」 |

### 1.2 UX 問題（「第一眼看不出能做什麼」）

1. 沒有上傳入口，一進站自動載 Demo。
2. 4 個 tab 中 2 個是行銷素材（NotebookLM 對照、簡報心智圖），與核心工具平級，主線被稀釋。
3. 沒有流程感，不知道現在在哪、下一步做什麼。
4. 全站幾乎都 `text-xs`，KPI 數字與說明文字幾乎同權重。
5. 缺空狀態 / 載入中 / 錯誤 / 無結果等狀態設計。
6. `MultiVersionTimeline` 全是寫死假資料，卻無「示範資料」標記，Demo Day 有誤導風險。

---

## 2. 設計主張：對話式 Agent 三欄

不用步驟精靈，改成**以對話為主軸的 agent 介面**——引導感來自 agent 主動敘事 + 建議動作 chips，而不是強制的線性步驟。

```
┌──────────────────────────────────────────────────────────────────────┐
│ TopBar   Plimsoll · 文件對 pill ······ [收左欄] [收右欄] [⚙️ 設定]     │
├────────────┬──────────────────────────────────┬──────────────────────┤
│ 對話紀錄    │        對話主區（中）              │  來源 ｜ 原文（右）   │
│ (可收合)    │  · agent 訊息 + 富卡片            │  · 本機資料夾／Drive  │
│ + 新的比對  │  · KPI 卡 / 差異卡（可就地覆核）    │  · 舊版／新版丟檔槽   │
│ 排序 · 拖曳 │  · 建議動作 chips                 │  · 原文頁 + 高亮命中  │
│ 本機/遠端tag│  ┌ composer 輸入框 ┐              ├──────────────────────┤
│            │                                  │  ● 流程節點（固定底部）│
└────────────┴──────────────────────────────────┴──────────────────────┘
```

四個原則：

- **對話是主線**：agent 每完成一件事就用一則訊息交代結果 + 給下一步建議 chips（「開始逐項覆核」「只看 4 筆 High」「先匯出報告」），使用者永遠知道能做什麼。
- **工作發生在對話裡**：差異卡直接展開就能判定（不用跳頁），保留完整 diff 對照與覆核控制。
- **右欄是證據**：點任何頁碼，右欄切到「原文」並跳到該頁、高亮命中句——這是「100% 可追溯」的視覺證據，取代第一版的雙欄 PDF viewer。
- **狀態在右下角，不在頂部**：流程節點固定在右欄底部（載入來源 → 差異比對 → 逐項覆核 → 簽核匯出），取代頂部步驟列；右欄收合後仍保留節點小圓點。

TopBar 只留品牌、文件對、雙欄收合鈕與齒輪。**角色、主題、語言、字型、高亮色、背景、API Key、資料來源全部收進設定齒輪**。

---

## 3. 三欄規格

### 3.1 左欄 `HistoryRail`（238px，可收合至 50px）

| 元件 | 規格 |
|---|---|
| `NewComparisonButton` | 黑底實心，開新的比對工作階段 |
| `HistorySearch` | 搜尋比對紀錄 |
| `SortBar` | **排序下拉**（最近更新／建立時間／名稱 A→Z／High 風險數／手動排序）＋ 升降冪切換鈕 |
| `HistoryList` | 依「今天 / 前 7 天 / 更早」分組。每列：拖曳握把、標題、`v1.0→v1.1`、差異數、High 數，**外加儲存位置 tag** |
| `UserFooter` | 頭像 + 姓名 + 角色 |
| 收合態 | 只留「＋」「展開」「紀錄」三顆圖示 + 頭像 |

**排序與拖曳**
- 條件排序：下拉選單選欄位，右側小鈕切換升／降冪，選擇存 `localStorage`。
- 手動排序：下拉選「手動排序」後，每列左側握把可拖曳（hover 才顯示，避免視覺噪音），拖曳中顯示插入位置指示線，來源列半透明並加虛線外框。
- 手動順序**只影響左欄顯示**，不動資料本身；切回條件排序時保留手動順序，可隨時切回。
- 實作用 HTML5 drag-and-drop 或 `@dnd-kit`；觸控裝置改為長按 400ms 觸發拖曳。

**儲存位置 tag（四種）**

| tag | 樣式 | 意義 |
|---|---|---|
| `本機` | 白底細框 | 只存在這台瀏覽器的 `localStorage`，不含 PDF 原檔 |
| `遠端` | 黑底白字 | 已寫入後端，含 PDF 原檔暫存，可跨裝置開啟 |
| `未同步` | 琥珀底 | 有本機變更尚未上傳（覆核狀態改過但沒同步） |
| `Drive` | 白底深框 | 來源檔案來自 Google 雲端硬碟（用在來源卡，非工作階段） |

- `本機` / `未同步` 項目 hover 出現「同步到伺服器」動作。
- 列表頂端在有本機項目時提示「N 筆比對僅存在本機，清除瀏覽器資料會消失」。
- tag 同時出現在：左欄每列、對話標題列、右欄流程狀態列表頭。
- 後端需求見 §5 B7。

### 3.2 中欄 `ConversationThread`

**`ThreadHeader`**：工作階段標題、狀態 pill、儲存位置 tag。**覆核進度不放這裡** —— 移到右欄底部的流程節點（見 §3.3）。

**`Thread`** 訊息型別：

| 型別 | 內容 |
|---|---|
| `text` | 一般 agent／使用者訊息 |
| `doc-info` | 文件資訊卡（兩份文件的標題、版本、日期、頁數、文字層品質） |
| `alert` | 缺件警示 / 掃描頁警示 / 未覆核 High 警示 |
| `overview` | KPI 四格（總差異／High／已覆核／待簽核 High）＋風險分佈條＋受影響設備 |
| `diff-list` | 差異卡列表，每張可展開 |
| `diff-detail` | 展開態：雙欄逐字 diff → 讀出區 → 覆核判定列 |
| `export` | 簽核資訊表單 + 匯出格式 |
| `visual-diff` | 視覺差異卡：四種檢視模式 + 變更區塊清單（見 §6） |
| `actions` | 建議動作 chips（永遠附在訊息尾端） |

**`DiffCard`（核心元件）**

- 收合態：風險 pill／變更類型／信心度／ID、一句話標題、**數值前後對照**（`85 °C` → `80 °C`）、受影響設備、`舊 p.4 → 新 p.4`（可點）
- 展開態：
  - 雙欄原文，**逐字級 diff**：`<del>` 紅刪除線 / `<ins>` 綠加粗
  - 讀出區：變更解讀、**觸發規則**（數值變更 / 安全關鍵詞 / 義務詞彙 — 讓風險分級可解釋）、建議處置
  - `ReviewBar`：三選一判定（已確認／需追蹤／不採納）＋**風險改判下拉**（人工覆蓋 AI 初判）＋覆核筆記＋「確認並前往下一筆」
  - 鍵盤：`J`/`K` 上下筆、`A`/`T`/`D` 判定、`Enter` 確認並跳下一筆

**`Composer`**：多行輸入、附件鈕（等同丟檔）、引用鈕（把目前差異帶入提問，顯示「已引用 D01」）、模型名稱、送出。上方為建議問題 chips，下方常駐免責聲明一行。RAG 回答**必須附 `D01 · 新 p.4` 形式的引用 pill**，點擊跳右欄對應頁。

### 3.3 右欄 `SidePanel`（344px，可收合至 46px）

兩個分頁 + 底部固定的流程節點。

**分頁一「來源」**
- **來源連線入口**（新增）：
  | 入口 | 實作 | tag |
  |---|---|---|
  | 選擇本機資料夾 | File System Access API（`showDirectoryPicker`），不支援的瀏覽器降級為 `<input webkitdirectory>` | `本機` |
  | 連結 Google 雲端資料夾 | Google Picker + Drive API **唯讀** scope（`drive.readonly` 或 `drive.file`），只讀取使用者指定的資料夾 | `Drive` |
  - 兩者都只列出資料夾內的 PDF，並提供搜尋與「指定為舊版／新版」動作
  - 明示「Plimsoll 只讀取你指定的資料夾，不會掃描其他位置」
- 空狀態：舊版 / 新版兩個 `Dropzone`（拖曳或點選，僅收 PDF）＋「開始版本差異比對」（未齊全時 disabled）
- 已載入：`FileCard` — 檔名、**來源 tag**、頁數（實際值）、大小、文字層品質、版本、日期、移除鈕
- **`MissingVersionField`**：版本號抓不到時就地補填，未填妥 → 比對按鈕維持 disabled（**系統不自行猜測版本號**）

**分頁二「原文」** — 三種呈現模式可切換

| 模式 | 說明 |
|---|---|
| **文字重建（預設）** | 用 `extract_pages()` 抽出的文字重排該頁。可高亮命中句（`mark.hit`）、可搜尋、可點擊跳轉。這是「100% 可追溯」最有說服力的呈現。 |
| **原始 PDF** | `<embed src="/api/pdf/{side}#page=n">` 嵌入原檔。排版 100% 忠實，但瀏覽器內建檢視器無法高亮特定句子，改在頁面上方以文字列出「本頁命中：…」。 |
| **視覺差異** | 光柵化後的像素比對，用於工程圖頁（見 §6）。 |

- 兩種模式在分頁上方以分段控制切換，預設「文字重建」，選擇記憶在 `localStorage`。
- 共通：舊版 / 新版 / 並排 三態、上下頁、「4 / 6 · 命中 2 處」。
- 「原始 PDF」模式需要後端暫存原檔（§5 B3）；`本機` 未同步的比對紀錄無 PDF 原檔時，此模式顯示「原檔未保存，請重新上傳或同步到伺服器」。

**底部固定 `FlowNodes`（流程節點）**

取代原本頂部的步驟列。直式四節點，永遠可見：

| 節點 | 狀態與副標 |
|---|---|
| 載入來源 | `v1.0 → v1.1 · 各 6 頁` ／ 待補件時轉紅 |
| 差異比對 | `25 筆 · 4 High · 8.2 秒` |
| 逐項覆核 | `18 / 25 已判定 · 2 High 待判` + 細進度條（綠＝已判、紅＝High 未判） |
| 簽核匯出 | `2 項 High 未覆核，將標為修訂草稿`（紅色警示態） |

- 四種節點狀態：`未開始`（灰）／`進行中`（黑，帶光暈）／`已完成`（綠勾）／`警示`（紅驚嘆）
- 節點可點擊，跳到對話中對應的訊息位置
- 表頭右側顯示該工作階段的儲存位置 tag
- **右欄收合至 46px 後，流程節點縮成一排小圓點**（保留顏色狀態），不必展開就知道進度
- 匯出前檢核清單與匯出格式改放在「來源」分頁下方（有結果時才出現）

### 3.4 `SettingsPanel`（右上齒輪）

**所有全域設定都在這裡**，桌面為 392px 浮層、手機為全螢幕頁。分五個分頁：

**① 一般**
| 區塊 | 內容 |
|---|---|
| 介面語言 | 繁體中文 / English |
| 外觀 | 淺色 / 深色 / 跟隨系統（見 §4.3） |
| 字型大小 | 小 / 標準 / 大 / 特大（見 §4.4） |
| 介面字體 | Inter+Noto Sans TC / IBM Plex Sans TC / 思源黑體 / 系統預設 |
| 原文段落字體 | Noto Sans TC / Noto Serif TC / IBM Plex Mono |
| 比對高亮顏色 | 經典 / 色盲友善 / 單色 / 自訂（見 §4.5） |
| 自訂背景 | 預設 / 純色 / 漸層 / 紋理 / 上傳圖片 + 模糊與遮罩滑桿。**只套用在畫布層**，卡片、文字與風險色不受影響，確保對比度 |
| 預設儲存位置 | 僅本機 / 自動同步到伺服器 |

**② 角色**（見 §3.5）

**③ AI 模型**
| 區塊 | 內容 |
|---|---|
| 分析模型 | Bedrock Claude 3.5 / OpenAI GPT-4o / Gemini / Groq / **不使用 LLM（本機規則引擎）** |
| **API Key / Token** | 密碼型輸入 + 「套用」。**狀態列**顯示「已啟用 XX LLM 模式」（綠燈）或「未設定，使用本機精準規則引擎」（灰燈）—— 這是第一版有、現行 React 版遺失的 F5。Key 只存瀏覽器工作階段，不寫入原始碼或版控 |
| 推論參數 | 唯讀顯示 `Temperature 0.5 / Top P 0.95 / Top K 40 / Repeat Penalty 1.1`（依工作包 S1000D 事實對齊規範） |

**④ 資料來源**
- 本機資料夾：顯示已授權路徑，可更換／移除
- Google 雲端硬碟：顯示已連線帳號與資料夾，可中斷授權
- 明示唯讀權限範圍

**⑤ 更多視圖**
- 多版本演進矩陣、簡報與心智圖、與 NotebookLM 的差異（皆接真實資料，見 §5 B9）

齒輪未設定 Key 時顯示提示小紅點。所有偏好存 `localStorage`（見 §5 B12）。

### 3.5 角色（設定 → 角色）

**角色從 TopBar 移進設定**，因為它不是高頻操作，放在頂部只會佔用視覺權重。不再整頁替換 dashboard（那正是功能被藏起來的原因）。角色只影響：

| | 安品主管 | 維修工程師 |
|---|---|---|
| 進站預設 | 總覽 KPI + 未覆核 High 摘要 | 直接進逐項覆核 |
| 差異排序 | 風險 × 未覆核優先 | 依文件頁序 |
| 右欄預設分頁 | 來源（含檢核清單） | 原文 |
| 匯出區 | 顯示簽核欄位 | 摺疊 |

**功能集合完全相同**，兩個角色都能存取全部能力。

---

## 4. 視覺系統

**原則：全站中性灰階、零色偏（Twenty 風格）。強調色就是黑（深色模式即白），沒有品牌彩色。**

唯一保留的彩色是**風險語意色**——High / Medium / Low 是命題明文要求的工安標示（「重大變更須以紅色高亮標示」），純黑白會讓這個核心價值消失。因此保留，但降飽和成 muted 色，且**只出現在**風險 pill、數值前後對照、警示框、覆核狀態這四種地方。

### 4.1 中性階

| Token | 淺色 | 深色 | 用途 |
|---|---|---|---|
| `ink-900` | `#000000` | `#FFFFFF` | 標題、強調數字 |
| `ink-800` / accent | `#171717` | `#EDEDED` | 主要按鈕底 / 深色模式主文 |
| `ink-700` | `#333333` | `#D6D6D6` | 主文 |
| `ink-600` | `#575757` | `#A5A5A5` | 內文 |
| `ink-500` | `#818181` | `#818181` | 次要文字 |
| `ink-400` | `#A5A5A5` | `#666666` | 標籤、頁碼 |
| `line` | `#E3E3E3` | `#2E2E2E` | 邊框 |
| `line-soft` | `#EDEDED` | `#242424` | 卡片內分隔 |
| `accent-soft` | `#F1F1F1` | `#282828` | 選中底、引用 pill |
| `canvas` | `#F5F5F5` | `#141414` | 畫布 |
| `surface-2` / `rail` | `#FAFAFA` | `#212121` / `#171717` | 次表面、左欄 |
| `surface` | `#FFFFFF` | `#1B1B1B` | 卡片、對話泡泡 |
| `on-accent` | `#FFFFFF` | `#0F0F0F` | 主要按鈕上的文字 |

### 4.2 風險語意色

| | 淺色 文字 / 標記 / 底 / 邊框 | 深色 文字 / 標記 / 底 / 邊框 |
|---|---|---|
| High | `#B3241B` / `#CE3A31` / `#FBF2F1` / `#F0CDCA` | `#F0837A` / `#D34A41` / `#2A1918` / `#5C2B27` |
| Medium | `#8E540C` / `#C07C1A` / `#FAF6EE` / `#EADCBE` | `#E3AE5E` / `#B98524` / `#251E12` / `#54401B` |
| Low | `#14603F` / `#248A5F` / `#F1F7F3` / `#C7E2D5` | `#63C79B` / `#2E9268` / `#152420` / `#24503E` |

### 4.3 其他

| 類別 | 規格 |
|---|---|
| 字階 | 28 / 22 / 18 / 15 / 13.5 / 12.5 / 11.5 共 7 級（取代全站 `text-xs`）；對話內文 13.5px |
| 字體 | Inter + Noto Sans TC + system fallback |
| 圓角 | 6 / 8 / 12 / full（比 v3 更收斂，貼近 Twenty） |
| 陰影 | 極輕。`sh-1` 卡片、`sh-2` 浮起、`sh-3` 彈窗；深色模式加深 alpha |
| 間距 | 4 / 8 / 12 / 16 / 24 / 32 |
| 主題實作 | CSS 變數 + `document.documentElement[data-theme]`；「跟隨系統」用 `prefers-color-scheme` media query；選擇存 `localStorage`；`color-scheme` 一併設定讓原生控制項跟著切換 |
| 無障礙 | 風險不只用顏色（同時有文字 pill）；焦點環 `0 0 0 3px accent-soft`；主要文字對比 ≥ 4.5:1，深色模式需重新驗證每個風險色 |

### 4.4 字型（設定 → 一般）

**字型大小** — 四段，整組字階等比縮放（`--fs` 乘數套在所有 `--t-*` 上），行高與 padding 同步調整避免擠版：

| 段 | 內文基準 | 乘數 |
|---|---|---|
| 小 | 12.5px | 0.93 |
| 標準（預設） | 13.5px | 1.00 |
| 大 | 15px | 1.11 |
| 特大 | 17px | 1.26 |

**介面字體** — 全部 OFL 開源可商用，**自架託管、不打外部 CDN**（離線環境與船上網路是實際需求）：

| 選項 | 說明 |
|---|---|
| Inter + Noto Sans TC（預設） | 中英混排最穩，字重齊全 |
| IBM Plex Sans TC | 工程感，數字辨識度高 |
| 思源黑體 Source Han Sans TC | 繁中字重最完整 |
| 系統預設 | 不載入網路字型，離線最快 |

**原文段落字體** — 條文原文可獨立設定，因為需求不同：

| 選項 | 適用 |
|---|---|
| Noto Sans TC（預設） | 與介面一致 |
| Noto Serif TC | 襯線，長段條文閱讀最省力 |
| IBM Plex Mono | 等寬，數值與零件料號逐字對齊比對 |

字型檔以 `woff2` 子集化後放 `frontend/public/fonts/`，用 `font-display: swap`，僅在使用者選到時才載入該字族（見 §5 B11）。

### 4.5 比對高亮顏色（設定 → 一般）

三種高亮可分別設定：**新增／新版內容**、**刪除／舊版內容**、**搜尋命中**。提供四組預設：

| 預設 | 新增 / 刪除 / 命中 | 用途 |
|---|---|---|
| 經典（預設） | 綠 `#248A5F` / 紅 `#CE3A31` / 黃 `#F3E6C4` | 一般情境 |
| 色盲友善 | 藍 `#1F6FEB` / 橘 `#D97706` / 淺藍 `#CFE3F7` | 紅綠色盲（deuteranopia / protanopia）安全 |
| 單色（無彩） | 灰階，靠粗細與底線區分 | 黑白列印、極簡偏好 |
| 自訂 | 任選色票 | — |

規則：
- **風險分級色（High / Medium / Low）跟著同一組色盤走**，不允許各自為政造成語意衝突。
- 選「單色（無彩）」時，風險改用**字重 + 圖示 + 左邊界**區分，仍符合命題「重大變更須明顯標示」的要求。
- 儲存前系統即時計算對比度，低於 3:1 的組合直接擋掉並提示。
- 設定同時套用到畫面、視覺差異的區塊框線，以及匯出的 HTML／PDF 報告。

### 4.6 RWD 斷點

策略是**「欄位變成分頁」**，不是把三欄硬壓縮：

| 斷點 | 佈局 |
|---|---|
| `≥1280px` | 完整三欄；左右欄可各自收合 |
| `768–1279px` | 兩欄：左欄自動收成 50px 圖示條，右欄改為抽屜（由 TopBar 鈕或點頁碼喚起）；流程節點從直式改為**頂部橫向精簡步驟條** |
| `<768px` | 單欄，對話為主畫面。左欄（紀錄）與右欄（來源）改為**底部分頁 + 上拉面板**（bottom sheet）；底部四個分頁：對話／來源／流程／紀錄，流程分頁帶未覆核 High 的紅色角標 |

手機額外規則：
- 覆核判定按鈕最小觸控高度 **44px**，三顆等寬平鋪
- 鍵盤快捷鍵提示（`J`/`K`/`A`）在觸控裝置隱藏
- 雙欄原文對照在 `<768px` 改為上下堆疊，並提供「只看新版／只看舊版」切換
- 上拉面板支援下滑關閉與吸附高度（40% / 78%）
- 設定面板在手機為全螢幕頁而非浮層

**RWD**
- `≥1280px`：三欄
- `900–1279px`：左欄自動收合成 52px
- `<900px`：單欄；左欄與右欄改為抽屜（右欄由 composer 的附件鈕與頁碼點擊喚起）

---

## 5. 後端 / 資料缺口

| # | 項目 | 現況 | 需求 |
|---|---|---|---|
| B1 | `POST /api/export-html` | **不存在**（前端已在呼叫 → 404） | 新增，重用第一版 `app.py:report_html()` |
| B2 | 頁面原文供右欄檢視 | `/api/compare` 只回 differences | 回傳 `old_pages` / `new_pages`（頁碼 + 文字），或新增 `GET /api/page-text` |
| B3 | PDF 原檔預覽 | 無 | 上傳後暫存 + `GET /api/pdf/{session_id}/{side}`，供「原始 PDF」模式嵌入 |
| B4 | 逐字級 diff | `comparison.py` 只回整句 old/new | 前端做 token-level diff，或後端回 `diff_spans` |
| B5 | 頁數 / 檔案大小 | 未回傳 | metadata 增加 `page_count`、`size_bytes` |
| B6 | 簽核資訊 | DOCX 匯出未帶 | `ExportRequest` 增加 `reviewer` / `date` / `dept` / `report_no` |
| B7 | 比對工作階段持久化 | 無 | 支撐左欄歷史紀錄的 `已同步` tag：`POST /api/sessions`、`GET /api/sessions`、`GET /api/sessions/{id}`、`DELETE /api/sessions/{id}`。含 PDF 原檔暫存與清理策略（例如 30 天）。`本機` tag 由前端 `localStorage` 負責 |
| B8 | i18n | 後端 `local_chat_answer` 已支援 language | 前端補字典（沿用第一版 `COPY` 結構） |
| B9 | 多版本比對（真實資料） | `MultiVersionTimeline.tsx` 全是寫死假資料 | 支援 3~5 份文件上傳：`POST /api/compare-multi` 回傳「條文 × 版本」矩陣（以章節/段落對齊為 key）。簡報與心智圖改吃真實 `differences`（`/api/slides-mindmap` 已存在，但目前 fallback 也是假資料，需確保有真實輸入時不落回 fallback） |
| B10 | **視覺差異比對** | `/api/vision-diff` 存在但**前端零呼叫**；專案**無 PDF 光柵化能力**（僅 `pypdf`）；無 key 時回寫死假資料 | 見 §6，需新增 `PyMuPDF` 依賴 + 像素 diff + 重寫端點 |
| B11 | 字型自架託管 | 無 | `frontend/public/fonts/` 放子集化 `woff2`（Inter、Noto Sans TC、Noto Serif TC、IBM Plex Sans TC、IBM Plex Mono、Source Han Sans TC），按使用者選擇動態載入，不打外部 CDN |
| B12 | 偏好設定持久化 | 無 | 語言／主題／背景／字型大小／字體／高亮色盤／左右欄收合／排序方式，統一存 `localStorage`；若已登入則同步到後端 user preferences |

---

## 6. 新功能：視覺差異比對（圖片快照）

### 6.1 現況（必須先講清楚）

| 項目 | 現況 |
|---|---|
| `POST /api/vision-diff` | 存在於 `backend/main.py:166`，但 `grep` 整個 `frontend/src/` **零個呼叫** |
| PDF → 圖片 | **完全沒有能力**。`requirements.txt` 只有 `streamlit / pypdf / reportlab`，`pypdf` 無法光柵化 |
| 端點行為 | 收兩張 base64 圖片，丟給 LLM 視覺模型「看圖說話」 |
| 無 API key 時 | `backend/main.py:170-174` 直接回**寫死假資料**（「新增迴流閥門 V-102」「8 bar」），且不標示為假 |
| 變更定位 | **沒有**。只有一段文字描述，沒有座標，無法在畫面上框出來 |

也就是說，這功能目前等於不存在。

### 6.2 要做需補的管線

```
PDF 頁 ──光柵化(PyMuPDF, 300dpi)──> PNG
                                     │
        新舊兩張 ──對齊(去歪斜/縮放正規化)──> 像素 diff
                                     │
                          連通區域分析 ──> 變更區塊座標 [x,y,w,h]
                                     │
                    只把「有變更的區塊」裁切後送 LLM ──> 區塊語意解讀
```

關鍵設計：**像素比對負責「哪裡變了」（客觀、可驗證），LLM 只負責「變了什麼」（推測、需人工確認）**。現行做法把兩件事都交給 LLM，所以沒有座標、也無法驗證。

### 6.3 UI 規格

四種檢視模式（右欄「原文」分頁新增第三個模式，或在對話流中以富卡片呈現）：

| 模式 | 說明 |
|---|---|
| 並排標框（預設） | 左右兩張快照，變更區塊用高亮色框出並標註 |
| 疊加 Onion-skin | 兩張疊合，可調透明度，看位移 |
| 滑桿 Swipe | 中線可拖曳，同一位置直接對照 |
| 差異遮罩 | 只顯示 diff 像素，其餘壓暗 |

- 變更區塊清單：每列一個區塊，含類型（新增／移除）、AI 解讀、風險等級，點擊在快照上聚焦
- 框線顏色沿用 §4.5 的高亮色盤
- **信心度固定標為「中」**，並在卡片內明示「區塊由像素比對產生（客觀），內容解讀由 AI 推測，必須人工確認」
- 明示誤報來源：排版位移、掃描歪斜
- 覆核判定的第三個選項從「不採納」改為「**誤報**」，語意更貼切
- 圖頁掃描結果清單放右欄，顯示每一頁的掃描狀態（有變更／無變更／校正中）

### 6.4 風險

- 光柵化 300 dpi 對長文件很吃 CPU 與記憶體，需背景工作 + 進度回報，不可同步阻塞
- 掃描頁歪斜校正若失敗，誤報率會很高，寧可標「無法可信比對」也不要輸出結論（對應工作包規則 R3）
- LLM 視覺解讀成本高，只送變更區塊裁切圖、不送整頁

---

## 7. 驗收情境對照

| AC | 命題要求 | 本設計對應 |
|---|---|---|
| AC1 | 載入兩份文件 → 顯示名稱/版本/日期/適用設備，缺失時警示 | 右欄 `FileCard` + 中欄 `doc-info` 卡 + `MissingVersionField` |
| AC2 | 至少 5 列差異，每列含雙版本、頁碼、原文 | `DiffCard` 展開態雙欄 diff + 右欄原文檢視 |
| AC3 | 安全數值/程序/適用條件變更 → 候選重大提示 + 受影響設備 + 建議動作 | `overview` 卡 + `DiffCard` 讀出區含觸發規則 |
| AC4 | 資訊不足 / 對齊低信心 → 標記未知並引導人工，不輸出未受支持結論 | 掃描頁警示、「信心度低」pill、缺版本號阻擋比對 |
| 情境四 | 未覆核 High 匯出時二次確認 | `UnreviewedExportGuard` Modal，逐項列出 |

---

## 8. 實作階段（確認設計後才動工）

| 階段 | 內容 | 產出 |
|---|---|---|
| P0 | Design tokens（CSS 變數 + Tailwind theme v4，含**淺／深雙主題**）、`AppShell` 三欄骨架、`TopBar`、`SettingsPopover`、`useSession` 狀態 | 骨架可跑，設定齒輪完成（F4/F5 回歸 + 深色模式） |
| P1 | 右欄「文件」分頁：Dropzone、FileCard、缺件補填、掃描警示 + 後端 B2/B5 | AC1 通過（F1/F3/F9 回歸） |
| P2 | 中欄對話框架：Thread、訊息型別、Composer、建議 chips、`overview` 卡 | AC3 通過 |
| P3 | `DiffCard` 完整態（逐字 diff + 覆核判定 + 鍵盤）＋ 右欄「原文來源」**雙模式**（文字重建 + 原始 PDF）+ 後端 B3/B4 | AC2 通過（F2 回歸） |
| P4 | 匯出：簽核表單、三格式、`UnreviewedExportGuard` + 後端 B1/B6 | AC4 + 情境四通過（F6/F7 回歸） |
| P5 | 左欄歷史紀錄（四種 tag + 排序 + 拖曳，B7）、i18n（B8） | 歷史紀錄可用 |
| P6 | **RWD**（§4.6 三個斷點、底部分頁、上拉面板、44px 觸控）、字型系統（§4.4、B11）、高亮色盤（§4.5） | 手機可用 |
| P7 | 來源連線：本機資料夾（File System Access API）＋ Google Drive 唯讀 Picker | 來源入口完成 |
| P8 | **視覺差異比對**（§6、B10）：PyMuPDF 光柵化 → 像素 diff → 區塊裁切送 LLM → 四種檢視模式 | 圖頁可比對 |
| P9 | 多版本矩陣與簡報心智圖接**真實資料**（B9，含 3~5 檔上傳）、E2E 測試更新 | 全綠 |

---

## 9. 已定案事項

| # | 決定 |
|---|---|
| 1 | **原文檢視**：文字重建（預設，可高亮命中句）＋ 原始 PDF 嵌入，兩種模式可切換 |
| 2 | **歷史紀錄**：`localStorage` 與後端持久化**並存**，列表用 `本機` / `已同步` tag 區分 |
| 3 | **多版本矩陣、簡報心智圖**：接真實資料，不留假資料（需 B9） |
| 4 | **配色**：黑白中性極簡（Twenty 風格），無品牌彩色；風險色保留但降飽和 |
| 5 | **深色模式**：做，淺色 / 深色 / 跟隨系統三態 |
| 6 | **產品名**：Plimsoll（載重線），logo 用載重線標記 |
| 7 | **狀態呈現**：右欄底部流程節點，取代頂部步驟列 |
| 8 | **TopBar 精簡**：角色、主題、語言、字型、高亮、背景、API Key、資料來源全部收進齒輪 |
| 9 | **雙欄可收合**：左右欄各自獨立收合，右欄收合後保留流程節點小圓點 |
| 10 | **左欄排序**：條件排序下拉 + 升降冪，或選「手動排序」後拖曳 |
| 11 | **儲存位置 tag**：`本機` / `遠端` / `未同步` / `Drive` 四種 |
| 12 | **來源連線**：本機資料夾 + Google 雲端資料夾（唯讀） |
| 13 | **自訂背景**：預設／純色／漸層／紋理／上傳圖片 + 模糊與遮罩 |
| 14 | **RWD**：三個斷點，欄位變分頁；手機 44px 觸控 |
| 15 | **字型設定**：四段大小 + 介面／原文字體各自可選，全 OFL 自架託管 |
| 16 | **高亮顏色可設定**：四組預設含色盲友善與單色，風險色跟著同一組色盤 |
| 17 | **視覺差異比對**：新功能，像素 diff 定位 + LLM 解讀（現行版等於不存在） |
| 18 | **分支**：程式碼在 `codex/260815`，commit 落在該分支，不碰 `main` |
